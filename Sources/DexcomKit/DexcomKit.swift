/// DexcomKit connects directly to a Dexcom G7 sensor as a passive follower,
/// running alongside the official Dexcom app paired on the same phone, and
/// streams glucose readings and connection state to any number of consumers.
public enum DexcomKitInfo {
    /// The current version of the package.
    public static let version = "0.0.1"
}
